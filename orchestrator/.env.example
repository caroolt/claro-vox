PORT=4002
DATABASE_URL=postgres://clarovox:clarovox_dev_pw@localhost:5432/claro_vox
CIV_URL=http://localhost:4001

# Opcional: se preenchida, o Orquestrador usa o Claude Haiku 4.5 de verdade
# (via SDK oficial da Anthropic) para classificar intenção e gerar a
# resposta. Sem a chave, cai automaticamente no motor baseado em regras
# (ver nlu.py) — suficiente para rodar a demonstração localmente sem
# depender de uma chave de API.
ANTHROPIC_API_KEY=
